import React, {Component} from 'react';
import './${NAME}.css';

class ${NAME} extends Component {
    render() {
        return (
            <div className="${NAME}-wrapper">

            </div>
        );
    }
}

export default ${NAME};